version: '3.8'

services:
  gold-silver-dashboard:
    build: .
    container_name: gold-silver-dashboard
    ports:
      - "3008:3008"
    environment:
      - PORT=3008
      - GOLD_API_KEY=${GOLD_API_KEY}
      - NEWS_API_KEY=${NEWS_API_KEY}
    restart: unless-stopped
